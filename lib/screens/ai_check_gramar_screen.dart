import 'package:flutter/material.dart';

class AiCheckGrammarScreen extends StatelessWidget {
  const AiCheckGrammarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("AiCheckGrammarScreen"),
    );
  }
}